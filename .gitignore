# NB: the order of these does matter when building exclusion lists
# Exclude first, then Include second.
# (Build an exclusion list; then build a list of things to exclude from the exclusions)

# Forget all of these
node_modules
assets/textures/*

# Remember all of these
!**/*.gitkeep
